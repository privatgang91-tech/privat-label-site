[build]
functions = "netlify/functions"
publish = "build"
command = "npm run build"

[functions]
node_bundler = "esbuild"

# Redirection vers les fonctions
[[redirects]]
from = "/.netlify/functions/*"
to = "/.netlify/functions/:splat"
status = 200